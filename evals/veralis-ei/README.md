# Veralis early-childhood evals

These cases are product-specific acceptance tests for private early-childhood schools. Runtime code must not import this directory or public fixture `ground_truth.json`.

The deterministic runner evaluates machine-checkable invariants such as bridge reconciliation, evidence presence, question count, conflicts, and metric availability. Language quality is reviewed separately so exact phrasing is not mistaken for financial correctness.

